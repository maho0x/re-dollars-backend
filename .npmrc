# pnpm configuration
# Auto-install peer dependencies to avoid manual installation
auto-install-peers=true

# Don't fail on peer dependency conflicts (more lenient)
strict-peer-dependencies=false

# Use proper node_modules structure (recommended for better compatibility)
shamefully-hoist=false

# Store location for global packages
store-dir=~/.pnpm-store