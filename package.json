{
  "name": "re-dollars-backend-next",
  "version": "0.1.0",
  "description": "Next-generation Bun backend for Re:Dollars with versioned REST APIs and native WebSocket transport.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "smoke": "bun scripts/smoke.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "@types/pg": "^8.16.0",
    "typescript": "^5.9.3"
  }
}
